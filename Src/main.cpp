#include "Card/Card.h"
using namespace std;
int main(){
	// Code
	// Client Ron;
	// Card A;
	// cin >> Ron;
	// Ron.setID("10220019");
	// cout << Ron;
	// cin >> A;
	// A.setHolder(Ron);
	// A.withdraw(3000);
	// string m;
	// cin >> m;
	// A.updatePin(A.getPin(), m);
	// A.updatePin(A.getPin(), m);
	// cout << A;
	return 0;
}